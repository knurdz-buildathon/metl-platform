apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane
  namespace: metl-system
  labels:
    app: control-plane
spec:
  replicas: 1
  selector:
    matchLabels:
      app: control-plane
  template:
    metadata:
      labels:
        app: control-plane
    spec:
      serviceAccountName: metl-workload-identity
      containers:
        - name: control-plane
          image: metl/control-plane:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: metl-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: control-plane
  namespace: metl-system
spec:
  selector:
    app: control-plane
  ports:
    - port: 80
      targetPort: 3000
