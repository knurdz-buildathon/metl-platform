apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-allocator
  namespace: metl-system
  labels:
    app: resource-allocator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resource-allocator
  template:
    metadata:
      labels:
        app: resource-allocator
    spec:
      serviceAccountName: metl-workload-identity
      containers:
        - name: resource-allocator
          image: metl/resource-allocator:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: metl-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: resource-allocator
  namespace: metl-system
spec:
  selector:
    app: resource-allocator
  ports:
    - port: 80
      targetPort: 3000
